import React from 'react';
import Container from '../../components/Shared/Container';

const Instructor = () => {
    return (
        <Container>
            <h2>Image, Name, Email</h2>
            <h3>Optional: Number of Class, and other</h3>
        </Container>
    );
};

export default Instructor;